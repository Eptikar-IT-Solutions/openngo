<%- title t("views.epp.links.edit %{model}", :model => t("views.locations.locations")) %>

<div class="col-lg-12">
	<div class="page-header">
		<h1><%= t("views.epp.links.edit %{model}", :model => t("views.locations.locations")) %></h1>
	</div>
	<%= render 'form' %>

</div>