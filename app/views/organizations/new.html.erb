<%- title t("views.epp.links.new %{model}", :model => t("views.organizations.organizations")) %>
<div class="col-lg-12">
	<div class="page-header">
		<h1><%= t("views.epp.links.new %{model}", :model => t("views.organizations.organizations")) %></h1>
	</div>
	<%= render 'form' %>

</div>
