<%- title "#{@role.name}" %>
<div class="col-lg-12 show">
	<div class="page-header">
		<h1><%= @role.name %></h1>
		<a title="Edit" href="<%= edit_role_path(@role) %>"><span class="glyphicon glyphicon-pencil"></span></a>
	</div>

	<div class="row">
		<div class="col-sm-2"><strong><%= t("views.roles.name") %></strong></div>


		<div class="col-sm-10"><%= @role.try(:name) || 'N/A' %></div>
	</div>

</div>