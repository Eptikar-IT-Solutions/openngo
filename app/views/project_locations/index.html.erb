<%- title t "views.application_layout.project_locations" %>
<div class="col-xs-12">

  <div class="page-header">
    <h1><%= t "views.application_layout.project_locations" %> <small><%= t("views.epp.Listing of all %{model}", :model => t("views.project_locations.project_locations")) %></small></h1>
  </div>


  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
        <tr>
              <th><%= t("views.project_locations.project_id") %></th>
              <th><%= t("views.project_locations.location_id") %></th>
              <th class="hidden-print"></th>
        </tr>
      </thead>

      <tbody>
        <% @project_locations.each do |project_location| %>
          <tr>
    
            <td><%= link_to project_location.project.name, project_location.project if project_location.project_id %></td>

            <td><%= link_to project_location.location.name, project_location.location if project_location.location_id %></td>
            <td class="hidden-print actions">
              <div class="btn-group">
                <%= link_to t('views.epp.links.show'), project_location , :class => 'btn btn-xs btn-default' %>
                <button type="button" class="btn btn-xs btn-default dropdown-toggle" data-toggle="dropdown">
                  <span class="caret"></span>
                  <span class="sr-only">Toggle Dropdown</span>
                </button>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to t('views.epp.links.edit'), edit_project_location_path(project_location) %></li>
                  <li><%= link_to t('views.epp.links.destroy'), project_location, method: :delete, data: { confirm: 'Are you sure?' } %></li>
                </ul>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <%= link_to t('views.epp.links.new %{model}', :model => t("views.project_locations.project_location")), new_project_location_path, :class => 'btn btn-primary btn-lg hidden-print' %>
</div>