<%= form_for(@project_partner, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @project_partner.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@project_partner.errors.count, "error") %> prohibited this project_partner from being saved:</h4>

      <ul>
        <% @project_partner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

          <div class="form-group">
        <%= f.label :project_id, :class => "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.number_field :project_id, :class => "form-control" %>
        </div>
      </div>
          <div class="form-group">
        <%= f.label :partner_id, :class => "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.number_field :partner_id, :class => "form-control" %>
        </div>
      </div>
        <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    </div>
    <% end %>