<%= form_for(@activity_location, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @activity_location.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@activity_location.errors.count, "error") %> prohibited this activity_location from being saved:</h4>

      <ul>
        <% @activity_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

          <div class="form-group">
        <%= f.label :activity_id, :class => "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.number_field :activity_id, :class => "form-control" %>
        </div>
      </div>
          <div class="form-group">
        <%= f.label :location_id, :class => "col-sm-2 control-label" %>
        <div class="col-sm-10">
          <%= f.number_field :location_id, :class => "form-control" %>
        </div>
      </div>
        <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    </div>
    <% end %>