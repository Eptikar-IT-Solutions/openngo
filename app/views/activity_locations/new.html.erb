<%- title t("views.epp.links.new %{model}", :model => t("views.activity_locations.activity_locations")) %>
<div class="col-lg-12">
	<div class="page-header">
		<h1><%= t("views.epp.links.new %{model}", :model => t("views.activity_locations.activity_locations")) %></h1>
	</div>
	<%= render 'form' %>

</div>
