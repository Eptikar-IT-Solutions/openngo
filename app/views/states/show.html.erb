<%- title "#{@state.name}" %>
<div class="col-lg-12 show">
	<div class="page-header">
		<h1><%= @state.name %></h1>
		<a title="Edit" href="<%= edit_state_path(@state) %>"><span class="glyphicon glyphicon-pencil"></span></a>
	</div>

	<div class="row">
		<div class="col-sm-2"><strong><%= t("views.states.name") %></strong></div>


		<div class="col-sm-10"><%= @state.try(:name) || 'N/A' %></div>
	</div>
</div>